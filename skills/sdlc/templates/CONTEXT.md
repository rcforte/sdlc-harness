# CONTEXT.md — <repo name> ubiquitous language

The shared glossary every spec, skill, and persona reads from. `/grill-with-docs` and
`/domain-modeling` keep it current. Keep it compact — it is loaded as context often.

> **Stub.** Populated as the domain emerges: the first `/sdlc feature` grilling for a
> greenfield repo, or `/sdlc onboard`'s `domain-modeling` pass for a brownfield one.

## Terms

| Term | Meaning |
|---|---|
| _Term_ | _one-line definition_ |

## Relationships

_How the core concepts relate — prose or ASCII, added as the model sharpens._
